# raw 资料归档说明

本目录按 `ref/ref.md` 的分类归档资料：

- `教程/`：教程网页离线包
- `框架/`：开源框架完整 Git clone
- `博客/`：博客、文章、公众号网页离线包
- `论文/`：论文 PDF
- `书籍/`：开源书籍完整 Git clone
- `合集/`：awesome 列表完整 Git clone

## 已完成

### 论文

- `论文/2604.08224.pdf`
- `论文/2603.03329.pdf`
- `论文/2604.25850.pdf`

### 开源项目

以下仓库均已补成完整 clone，`git rev-parse --is-shallow-repository` 返回 `false`：

- `框架/pi`
- `书籍/harness-engineering-from-cc-to-ai-coding`
- `书籍/harness-books`
- `合集/awesome-agent-harness`
- `合集/awesome-harness-engineering`

注：直连 GitHub 一度超时，完整化时使用了 `gh-proxy.com` 作为临时下载通道；仓库 `origin` 已设回原 GitHub 地址。

### 网页与图片

每条网页资料单独建目录，目录内包含：

- `source-url.txt`：原始 URL
- `wget.log` 或 `wget-retry.log`：抓取日志
- 离线 HTML
- 页面引用的图片、CSS、脚本等资源

已抓到正文页面和图片资源的主要目录：

- `教程/learn-shareai-run-zh`
- `博客/anthropic-harness-design-long-running-apps`
- `博客/woshipm-ai-6401079`
- `博客/bestblogs-b4f0ee9e`
- `博客/openai-harness-engineering`
- `博客/addyosmani-agent-harness-engineering`
- `博客/ltbase-agents-harness-engineering`
- `博客/weixin-mgf8k7xzrtkod0pmoing3w`
- `教程/walkinglabs-learn-harness-engineering-en`
- `博客/diegopachecotech-substack-harness-engineering`

### X / Twitter

用户已明确同意使用非官方、反向工程接口抓取 X 内容。由于本机 Chrome 登录态不可用，`baoyu-danger-x-to-markdown` 未能通过官方脚本取得 cookies；实际抓取改用 `api.fxtwitter.com` 保存公开推文/文章数据。

成果目录：`博客/x-tweets/`

每条 X 资料按 `作者/status_id/` 建目录，包含：

- `source-url.txt`：原始 X 链接
- `api-url.txt`：抓取用 API 链接
- `fxtwitter.json`：原始 JSON
- `content.md`：整理后的 Markdown
- `all-urls.txt`：JSON 中出现的全部 URL
- `media-urls.txt`：识别出的图片/视频 URL
- `media/`：已下载图片/视频
- `curl.log`、`media-download.log`、`media-retry.log`：抓取日志

本次 `ref/ref.md` 中 12 条 X 链接均已保存 JSON 和 Markdown；共下载 X 相关媒体 98 个文件。`yan5xu/2059117572826746979` 是 X Article，已保存完整正文，API 未返回 `pbs.twimg.com/media` 图片，因此 `media/` 为空。

### Medium

Medium 直连抓取曾超时。用户在本机 Chrome 登录 Medium，并开启 `Allow JavaScript from Apple Events` 后，已通过已登录 Chrome 渲染页面并抓取 9 条 Medium 内容。

每条 Medium 目录包含：

- `source-url.txt`：原始链接
- `chrome-captured.html`：通过已登录 Chrome 捕获的渲染 HTML
- `content.md`：从渲染 HTML 转换出的 Markdown
- `media-urls.txt`：Markdown 中识别出的 Medium 图片 URL
- `media/`：通过图片代理下载的 Medium 图片
- `media-map.tsv`、`media-download.log`：图片 URL 与本地文件对应关系、下载日志

本次临时使用过两个辅助脚本，位于 `raw/` 根目录，但按规则不入库；如需复用，可参考 `抓取技巧.md` 重建：

- `capture_chrome_html.applescript`：从已登录 Chrome 捕获渲染 HTML
- `medium_html_to_markdown.ts`：将捕获 HTML 转为 Markdown

## 抓取失败或受限

无。`besthub-six-pillars` 已从 `ref/ref.md` 移除，不再属于本次目标清单；失败抓取目录仍保留在 `博客/besthub-six-pillars/`，用于追溯 HTTP 403 原因。

## 待确认

无。
